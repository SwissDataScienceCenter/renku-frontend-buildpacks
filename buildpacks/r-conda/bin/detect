#!/usr/bin/env bash
set -eo pipefail

plan_path="${CNB_BUILD_PLAN_PATH}"

cat >>"${plan_path}" <<EOL
[[provides]]
name = "r"

[[requires]]
name = "conda"  # provided by "paketo-buildpacks/miniconda"

[requires.metadata]
build  = true
launch = false
EOL
