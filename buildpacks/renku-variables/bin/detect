#!/usr/bin/env bash
set -eo pipefail

echo "PASS: Always apply the Renku environment variables manager."

# 1. GET ARGS
plan_path=$2

# 2. DECLARE DEPENDENCIES (OPTIONAL)

cat >>"${plan_path}" <<EOL
[[provides]]
name = "renku-env"
EOL
